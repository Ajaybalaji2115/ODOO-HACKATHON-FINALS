package com.example.skillforge.model.enums;

/**
 * Enum to define who can see/view a course
 */
public enum CourseVisibility {
    EVERYONE, // Anyone can see the course (including guests)
    SIGNED_IN // Only logged-in users can see the course
}
